This is an Alx project called Airbnb clone
It has two storage engines: File storage and MYSQL storage
It will use a web framework called Flask to dispaly content on the client side(Front-end)

We will use a command line interpreter using the cmd module
